-- Сервисы local Players = game:GetService("Players") local RunService = game:GetService("RunService") local UserInputService = game:GetService("UserInputService") local TweenService = game:GetService("TweenService") local CoreGui = game:GetService("CoreGui") local HttpService = game:GetService("HttpService") -- Локальный игрок local localPlayer = Players.LocalPlayer -- Основные переменные local espCache = {} local connections = {} local screenGui = nil local chamsCache = {} local isMinimized = false local isMenuVisible = true local mainFrame = nil local originalFOV = 70 local xrayCache = {} local droneCache = {} local lastUpdate = 0 local UPDATE_INTERVAL = 0.033 -- 30 FPS для максимальной плавности -- Кэш для часто используемых объектов local camera = workspace.CurrentCamera local teamsFolder = workspace:FindFirstChild("___teams_") -- Настройки local Settings = { ESP = { Enabled = true, ShowEnemies = true, ShowDrones = true, MaxDistance = 1000, ShowNames = true, DroneDistance = 500 }, Visuals = { ShowDistance = true, ShowHealth = true, OutlineModels = true, Chams = false, ChamsColor = Color3.fromRGB(255, 255, 255), ChamsTransparency = 0.3, XRay = false, XRayTransparency = 0.5 }, FOV = { Enabled = false, Value = 70, MaxFOV = 120 } } -- Цветовая палитра (современный черно-белый дизайн) local COLORS = { Background = Color3.fromRGB(10, 10, 10), Surface = Color3.fromRGB(20, 20, 20), Primary = Color3.fromRGB(255, 255, 255), Secondary = Color3.fromRGB(180, 180, 180), Accent = Color3.fromRGB(220, 220, 220), Border = Color3.fromRGB(50, 50, 50), Success = Color3.fromRGB(100, 255, 100), Warning = Color3.fromRGB(255, 255, 100), Error = Color3.fromRGB(255, 100, 100) } -- Оптимизированная функция для создания интерфейса local function createGUI() if screenGui then screenGui:Destroy() end screenGui = Instance.new("ScreenGui") screenGui.Name = "WinterESP" screenGui.Parent = CoreGui screenGui.DisplayOrder = 999 screenGui.ResetOnSpawn = false return screenGui end -- Оптимизированная функция для получения команды игрока local function getPlayerTeam(character) if not character or not character.Parent then return nil end if not teamsFolder then teamsFolder = workspace:FindFirstChild("___teams_") if not teamsFolder then return nil end end for _, teamModel in pairs(teamsFolder:GetChildren()) do if teamModel:IsA("Model") and (teamModel.Name:lower() == "attackers" or teamModel.Name:lower() == "defenders") then for _, player in pairs(teamModel:GetChildren()) do if player:IsA("Model") and player.Name == character.Name then local humanoid = player:FindFirstChildOfClass("Humanoid") if humanoid then return teamModel.Name end end end end end return nil end -- Функция для определения противоположной команды local function getOppositeTeam(myTeam) if myTeam:lower() == "defenders" then return "attackers" elseif myTeam:lower() == "attackers" then return "defenders" end return nil end -- Улучшенный поиск FPV дронов которые летают local function findFPVDrones() local drones = {} -- Ищем основную модель дрона по GUID local droneModel = workspace:FindFirstChild("2bba961d-890f-4230-b386-783adeb648ce") if droneModel and droneModel:IsA("Model") then -- Ищем все модели внутри основной модели дрона for _, child in pairs(droneModel:GetChildren()) do if child:IsA("Model") then -- Ищем любые базовые части в модели дрона local rootPart = child:FindFirstChild("HumanoidRootPart") or child:FindFirstChild("Head") or child.PrimaryPart if not rootPart then -- Если нет стандартных частей, ищем любую базовую часть for _, part in pairs(child:GetDescendants()) do if part:IsA("BasePart") then rootPart = part break end end end if rootPart then -- Проверяем что дрон активен (высота над землей или имеет скорость) local isFlying = rootPart.Position.Y > 5 or rootPart.AssemblyLinearVelocity.Magnitude > 1 if isFlying and not drones[child] then drones[child] = { Object = child, Type = "Drone", Name = "FPV Дрон", RootPart = rootPart } end end end end end -- Дополнительный поиск дронов по всему workspace for _, obj in pairs(workspace:GetChildren()) do if obj:IsA("Model") then local nameLower = obj.Name:lower() -- Ищем по характерным названиям дронов if nameLower:find("drone") or nameLower:find("fpv") or nameLower:find("quadcopter") or nameLower:find("uav") then local rootPart = obj:FindFirstChild("HumanoidRootPart") or obj:FindFirstChild("Head") or obj.PrimaryPart if not rootPart then for _, part in pairs(obj:GetDescendants()) do if part:IsA("BasePart") then rootPart = part break end end end if rootPart and not drones[obj] then local isFlying = rootPart.Position.Y > 5 or rootPart.AssemblyLinearVelocity.Magnitude > 1 if isFlying then drones[obj] = { Object = obj, Type = "Drone", Name = "FPV Дрон", RootPart = rootPart } end end end -- Ищем по характерным частям дронов for _, part in pairs(obj:GetDescendants()) do if part:IsA("BasePart") then local partNameLower = part.Name:lower() if partNameLower:find("fpv") or partNameLower:find("camera") or partNameLower:find("rotor") or partNameLower:find("propeller") then local parentModel = part:FindFirstAncestorOfClass("Model") if parentModel and not drones[parentModel] then local rootPart = parentModel:FindFirstChild("HumanoidRootPart") or parentModel.PrimaryPart or part if rootPart then local isFlying = rootPart.Position.Y > 5 or rootPart.AssemblyLinearVelocity.Magnitude > 1 if isFlying then drones[parentModel] = { Object = parentModel, Type = "Drone", Name = "FPV Дрон", RootPart = rootPart } end end end end end end end end -- Отладочная информация if Settings.ESP.ShowDrones then local droneCount = 0 for _ in pairs(drones) do droneCount = droneCount + 1 end if droneCount > 0 then print("🔍 Найдено FPV дронов: " .. droneCount) end end return drones end -- Оптимизированный поиск врагов и дронов local function findTargets() local targets = {} local localCharacter = localPlayer.Character if not localCharacter then return targets end local myTeam = getPlayerTeam(localCharacter) -- Поиск вражеских игроков if myTeam then local enemyTeam = getOppositeTeam(myTeam) if enemyTeam and teamsFolder then local enemyTeamModel = teamsFolder:FindFirstChild(enemyTeam) if enemyTeamModel and enemyTeamModel:IsA("Model") then for _, character in pairs(enemyTeamModel:GetChildren()) do if character:IsA("Model") and character ~= localCharacter then local humanoid = character:FindFirstChildOfClass("Humanoid") if humanoid and humanoid.Health > 0 then targets[character] = { Object = character, Type = "Enemy", Name = character.Name } end end end end end end -- Поиск FPV дронов if Settings.ESP.ShowDrones then local drones = findFPVDrones() for drone, droneData in pairs(drones) do targets[drone] = droneData end end return targets end -- Оптимизированная функция для применения X-Ray local function applyXRay() if not Settings.Visuals.XRay then -- Восстанавливаем прозрачность только для измененных частей for part, originalTransparency in pairs(xrayCache) do if part and part.Parent then pcall(function() part.Transparency = originalTransparency end) end end xrayCache = {} return end -- Применяем X-Ray только к новым частям local currentTime = tick() for _, obj in pairs(workspace:GetDescendants()) do if obj:IsA("BasePart") and obj.Transparency < Settings.Visuals.XRayTransparency then if not xrayCache[obj] then xrayCache[obj] = obj.Transparency end pcall(function() obj.Transparency = Settings.Visuals.XRayTransparency end) end end end -- Оптимизированная функция для применения Chams local function applyChams(target) if not target or not target.Parent then return end if chamsCache[target] then chamsCache[target]:Destroy() chamsCache[target] = nil end if not Settings.Visuals.Chams then return end local highlight = Instance.new("Highlight") highlight.Name = "Chams" highlight.Adornee = target highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop highlight.FillColor = Settings.Visuals.ChamsColor highlight.OutlineColor = Settings.Visuals.ChamsColor highlight.FillTransparency = Settings.Visuals.ChamsTransparency highlight.OutlineTransparency = 0 highlight.Parent = target chamsCache[target] = highlight end -- Функция для удаления Chams local function removeChams(target) if chamsCache[target] then chamsCache[target]:Destroy() chamsCache[target] = nil end end -- Оптимизированное создание обводки модели local function createModelOutline(target) if not target then return nil end local highlight = Instance.new("Highlight") highlight.Name = "ESP_Outline" highlight.Adornee = target.Object highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop highlight.FillTransparency = 0.95 highlight.OutlineColor = Settings.Visuals.ChamsColor highlight.OutlineTransparency = 0 highlight.FillColor = Settings.Visuals.ChamsColor highlight.Parent = target.Object return highlight end -- Ультра-плавное обновление позиции ESP с максимальной стабильностью local function smoothUpdatePosition(espFrame, targetPosition, alpha) if not espFrame or not targetPosition then return end local currentPosition = espFrame.Position -- Используем экспоненциальное сглаживание для максимальной плавности local newX = currentPosition.X.Offset + (targetPosition.X.Offset - currentPosition.X.Offset) * alpha local newY = currentPosition.Y.Offset + (targetPosition.Y.Offset - currentPosition.Y.Offset) * alpha -- Ограничиваем максимальную скорость движения для стабильности local maxSpeed = 50 -- пикселей за кадр local deltaX = newX - currentPosition.X.Offset local deltaY = newY - currentPosition.Y.Offset if math.abs(deltaX) > maxSpeed then newX = currentPosition.X.Offset + (deltaX > 0 and maxSpeed or -maxSpeed) end if math.abs(deltaY) > maxSpeed then newY = currentPosition.Y.Offset + (deltaY > 0 and maxSpeed or -maxSpeed) end espFrame.Position = UDim2.new(0, newX, 0, newY) end -- Плавное изменение размера линии здоровья local function smoothHealthUpdate(healthFill, targetSize, alpha) if not healthFill then return end local currentSize = healthFill.Size local newWidth = currentSize.X.Scale + (targetSize.X.Scale - currentSize.X.Scale) * alpha healthFill.Size = UDim2.new(newWidth, 0, 1, 0) end -- Оптимизированное создание ESP элемента local function createESP(target) if espCache[target.Object] then return end local espFrame = Instance.new("Frame") espFrame.Name = "ESP_" .. target.Object.Name espFrame.BackgroundTransparency = 1 espFrame.Size = UDim2.new(1, 0, 1, 0) espFrame.ZIndex = 10 espFrame.Visible = false espFrame.Parent = screenGui local outline = Settings.Visuals.OutlineModels and createModelOutline(target) if Settings.Visuals.Chams then applyChams(target.Object) end -- Улучшенная панель информации с линией здоровья local infoPanel = Instance.new("Frame") infoPanel.Name = "InfoPanel" infoPanel.BackgroundTransparency = 0.85 infoPanel.BackgroundColor3 = Color3.fromRGB(0, 0, 0) infoPanel.Size = UDim2.new(0, 120, 0, 45) infoPanel.ZIndex = 12 infoPanel.Visible = Settings.ESP.ShowNames infoPanel.Parent = espFrame local UICorner = Instance.new("UICorner") UICorner.CornerRadius = UDim.new(0, 6) UICorner.Parent = infoPanel local nameLabel = Instance.new("TextLabel") nameLabel.Name = "NameLabel" nameLabel.Text = target.Name nameLabel.TextColor3 = target.Type == "Drone" and COLORS.Warning or Settings.Visuals.ChamsColor nameLabel.BackgroundTransparency = 1 nameLabel.Size = UDim2.new(1, -8, 0, 18) nameLabel.Position = UDim2.new(0, 4, 0, 2) nameLabel.Font = Enum.Font.GothamBold nameLabel.TextSize = 11 nameLabel.TextXAlignment = Enum.TextXAlignment.Left nameLabel.ZIndex = 13 nameLabel.Parent = infoPanel local distanceLabel = Instance.new("TextLabel") distanceLabel.Name = "DistanceLabel" distanceLabel.Text = "0m" distanceLabel.TextColor3 = COLORS.Secondary distanceLabel.BackgroundTransparency = 1 distanceLabel.Size = UDim2.new(1, -8, 0, 14) distanceLabel.Position = UDim2.new(0, 4, 0, 20) distanceLabel.Font = Enum.Font.Gotham distanceLabel.TextSize = 10 distanceLabel.TextXAlignment = Enum.TextXAlignment.Left distanceLabel.ZIndex = 13 distanceLabel.Visible = Settings.Visuals.ShowDistance distanceLabel.Parent = infoPanel -- Линия здоровья вместо текста local healthBar = Instance.new("Frame") healthBar.Name = "HealthBar" healthBar.BackgroundColor3 = COLORS.Surface healthBar.BorderSizePixel = 0 healthBar.Size = UDim2.new(1, -8, 0, 4) healthBar.Position = UDim2.new(0, 4, 0, 36) healthBar.ZIndex = 12 healthBar.Visible = Settings.Visuals.ShowHealth and target.Type ~= "Drone" healthBar.Parent = infoPanel local healthCorner = Instance.new("UICorner") healthCorner.CornerRadius = UDim.new(1, 0) healthCorner.Parent = healthBar local healthFill = Instance.new("Frame") healthFill.Name = "HealthFill" healthFill.BorderSizePixel = 0 healthFill.Size = UDim2.new(1, 0, 1, 0) healthFill.ZIndex = 13 healthFill.Parent = healthBar local fillCorner = Instance.new("UICorner") fillCorner.CornerRadius = UDim.new(1, 0) fillCorner.Parent = healthFill espCache[target.Object] = { Frame = espFrame, Target = target, Outline = outline, LastUpdate = 0, SmoothPosition = UDim2.new(0, 0, 0, 0), LastScreenPos = Vector2.new(0, 0), HealthPercent = 1.0 } end -- Оптимизированное обновление ESP с ультра-плавностью и стабильностью local function updateESP(targetData) local target = targetData.Target.Object local espFrame = targetData.Frame if not target or not target.Parent then espFrame.Visible = false if targetData.Outline then targetData.Outline:Destroy() end removeChams(target) return false end local rootPart = target:FindFirstChild("HumanoidRootPart") or target:FindFirstChild("Head") or target.PrimaryPart or targetData.Target.RootPart if not rootPart then espFrame.Visible = false if targetData.Outline then targetData.Outline:Destroy() end removeChams(target) return false end local success, screenPos, onScreen = pcall(function() return camera:WorldToViewportPoint(rootPart.Position) end) if not success then espFrame.Visible = false if targetData.Outline then targetData.Outline:Destroy() end removeChams(target) return false end local distance = (camera.CFrame.Position - rootPart.Position).Magnitude local maxDistance = targetData.Target.Type == "Drone" and Settings.ESP.DroneDistance or Settings.ESP.MaxDistance if Settings.ESP.Enabled and onScreen and distance <= maxDistance then if targetData.Target.Type == "Enemy" and not Settings.ESP.ShowEnemies then espFrame.Visible = false return false end if targetData.Target.Type == "Drone" and not Settings.ESP.ShowDrones then espFrame.Visible = false return false end if Settings.Visuals.Chams then applyChams(target) else removeChams(target) end if targetData.Outline then targetData.Outline.Enabled = Settings.Visuals.OutlineModels targetData.Outline.OutlineColor = targetData.Target.Type == "Drone" and COLORS.Warning or Settings.Visuals.ChamsColor targetData.Outline.FillColor = targetData.Target.Type == "Drone" and COLORS.Warning or Settings.Visuals.ChamsColor end local infoPanel = espFrame:FindFirstChild("InfoPanel") if infoPanel then -- Плавное обновление позиции с максимальной стабильностью local targetPosition = UDim2.new(0, screenPos.X - 60, 0, screenPos.Y - 50) -- Инициализация позиции если это первый кадр if targetData.SmoothPosition == UDim2.new(0, 0, 0, 0) then targetData.SmoothPosition = targetPosition infoPanel.Position = targetPosition else -- Ультра-плавное следование с разными коэффициентами для разных ситуаций local alpha = 0.3 -- Медленное сглаживание для максимальной плавности -- Если позиция сильно изменилась (резкий поворот камеры), используем более быстрое следование local posDelta = math.abs(targetPosition.X.Offset - targetData.SmoothPosition.X.Offset) + math.abs(targetPosition.Y.Offset - targetData.SmoothPosition.Y.Offset) if posDelta > 100 then -- Большое изменение позиции alpha = 0.6 -- Быстрее реагируем на резкие движения elseif posDelta > 50 then -- Среднее изменение alpha = 0.4 -- Умеренная скорость end smoothUpdatePosition(infoPanel, targetPosition, alpha) targetData.SmoothPosition = infoPanel.Position end infoPanel.Visible = Settings.ESP.ShowNames local nameLabel = infoPanel:FindFirstChild("NameLabel") local distanceLabel = infoPanel:FindFirstChild("DistanceLabel") local healthBar = infoPanel:FindFirstChild("HealthBar") if nameLabel then nameLabel.Text = targetData.Target.Name nameLabel.TextColor3 = targetData.Target.Type == "Drone" and COLORS.Warning or Settings.Visuals.ChamsColor end if distanceLabel then distanceLabel.Text = math.floor(distance) .. "m" distanceLabel.Visible = Settings.Visuals.ShowDistance distanceLabel.TextColor3 = targetData.Target.Type == "Drone" and COLORS.Warning or COLORS.Secondary end if healthBar then if targetData.Target.Type == "Enemy" then local humanoid = target:FindFirstChildOfClass("Humanoid") if humanoid and humanoid.MaxHealth > 0 then local newHealthPercent = math.clamp(humanoid.Health / humanoid.MaxHealth, 0, 1) -- Плавное изменение здоровья local healthAlpha = 0.5 -- Медленное изменение здоровья для плавности targetData.HealthPercent = targetData.HealthPercent + (newHealthPercent - targetData.HealthPercent) * healthAlpha local healthFill = healthBar:FindFirstChild("HealthFill") if healthFill then -- Плавное изменение ширины линии здоровья smoothHealthUpdate(healthFill, UDim2.new(targetData.HealthPercent, 0, 1, 0), 0.8) -- Градиент цвета от красного к зеленому if targetData.HealthPercent > 0.5 then healthFill.BackgroundColor3 = Color3.fromRGB( (1 - targetData.HealthPercent) * 255 * 2, 255, 0 ) else healthFill.BackgroundColor3 = Color3.fromRGB( 255, targetData.HealthPercent * 255 * 2, 0 ) end end healthBar.Visible = Settings.Visuals.ShowHealth else healthBar.Visible = false end else healthBar.Visible = false end end end espFrame.Visible = true targetData.LastUpdate = tick() targetData.LastScreenPos = Vector2.new(screenPos.X, screenPos.Y) return true else espFrame.Visible = false if targetData.Outline then targetData.Outline.Enabled = false end removeChams(target) return false end end -- Оптимизированный основной цикл ESP local function updateESPSystem() local currentTime = tick() -- Обновляем с фиксированным интервалом для максимальной плавности if currentTime - lastUpdate < UPDATE_INTERVAL then return end lastUpdate = currentTime if not Settings.ESP.Enabled then for target, data in pairs(espCache) do data.Frame.Visible = false if data.Outline then data.Outline.Enabled = false end removeChams(target) end return end local targets = findTargets() local currentTargets = {} -- Оптимизация: используем пары только один раз for target, targetData in pairs(targets) do if not espCache[target] then createESP(targetData) end currentTargets[target] = true end -- Удаляем старые цели for target, data in pairs(espCache) do if not currentTargets[target] then data.Frame:Destroy() if data.Outline then data.Outline:Destroy() end removeChams(target) espCache[target] = nil end end -- Обновляем видимые цели for target, data in pairs(espCache) do updateESP(data) end end -- Функция для изменения FOV local function updateFOV() if camera then if Settings.FOV.Enabled then camera.FieldOfView = Settings.FOV.Value else camera.FieldOfView = originalFOV end end end -- Улучшенная функция для создания переключателей local function createToggle(name, settingTable, settingKey, yPos, parentFrame) local toggleFrame = Instance.new("Frame") toggleFrame.Size = UDim2.new(1, -20, 0, 35) toggleFrame.Position = UDim2.new(0, 10, 0, yPos) toggleFrame.BackgroundColor3 = COLORS.Surface toggleFrame.BackgroundTransparency = 0.1 toggleFrame.Parent = parentFrame local toggleCorner = Instance.new("UICorner") toggleCorner.CornerRadius = UDim.new(0, 8) toggleCorner.Parent = toggleFrame local toggleStroke = Instance.new("UIStroke") toggleStroke.Color = COLORS.Border toggleStroke.Thickness = 1 toggleStroke.Parent = toggleFrame local label = Instance.new("TextLabel") label.Text = name label.Size = UDim2.new(0.7, 0, 1, 0) label.BackgroundTransparency = 1 label.TextColor3 = COLORS.Primary label.Font = Enum.Font.GothamBold label.TextSize = 13 label.TextXAlignment = Enum.TextXAlignment.Left label.TextYAlignment = Enum.TextYAlignment.Center label.Parent = toggleFrame -- Контейнер для переключателя local switchContainer = Instance.new("Frame") switchContainer.Size = UDim2.new(0, 50, 0, 25) switchContainer.Position = UDim2.new(0.85, -25, 0.5, -12) switchContainer.BackgroundColor3 = COLORS.Surface switchContainer.Parent = toggleFrame local switchCorner = Instance.new("UICorner") switchCorner.CornerRadius = UDim.new(1, 0) switchCorner.Parent = switchContainer local switchStroke = Instance.new("UIStroke") switchStroke.Color = COLORS.Border switchStroke.Thickness = 2 switchStroke.Parent = switchContainer -- Внутренний круг переключателя local switchCircle = Instance.new("Frame") switchCircle.Size = UDim2.new(0, 19, 0, 19) switchCircle.Position = UDim2.new(0, 3, 0.5, -9) switchCircle.BackgroundColor3 = settingTable[settingKey] and COLORS.Success or COLORS.Error switchCircle.Parent = switchContainer local circleCorner = Instance.new("UICorner") circleCorner.CornerRadius = UDim.new(1, 0) circleCorner.Parent = switchCircle local circleStroke = Instance.new("UIStroke") circleStroke.Color = COLORS.Primary circleStroke.Thickness = 1 circleStroke.Parent = switchCircle local toggleButton = Instance.new("TextButton") toggleButton.Text = "" toggleButton.Size = UDim2.new(1, 0, 1, 0) toggleButton.BackgroundTransparency = 1 toggleButton.Parent = toggleFrame -- Функция анимации переключателя local function animateToggle(isEnabled) local targetPosition = isEnabled and UDim2.new(1, -22, 0.5, -9) or UDim2.new(0, 3, 0.5, -9) local targetColor = isEnabled and COLORS.Success or COLORS.Error local tweenInfo = TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out) local positionTween = TweenService:Create(switchCircle, tweenInfo, {Position = targetPosition}) local colorTween = TweenService:Create(switchCircle, tweenInfo, {BackgroundColor3 = targetColor}) positionTween:Play() colorTween:Play() end -- Инициализация состояния animateToggle(settingTable[settingKey]) toggleButton.MouseButton1Click:Connect(function() settingTable[settingKey] = not settingTable[settingKey] animateToggle(settingTable[settingKey]) if settingTable == Settings.FOV then updateFOV() elseif settingKey == "XRay" then applyXRay() end end) return toggleFrame end -- Функция для создания слайдера local function createSlider(name, settingTable, settingKey, minValue, maxValue, yPos, parentFrame) local sliderFrame = Instance.new("Frame") sliderFrame.Size = UDim2.new(1, -20, 0, 50) sliderFrame.Position = UDim2.new(0, 10, 0, yPos) sliderFrame.BackgroundColor3 = COLORS.Surface sliderFrame.BackgroundTransparency = 0.1 sliderFrame.Parent = parentFrame local sliderCorner = Instance.new("UICorner") sliderCorner.CornerRadius = UDim.new(0, 8) sliderCorner.Parent = sliderFrame local sliderStroke = Instance.new("UIStroke") sliderStroke.Color = COLORS.Border sliderStroke.Thickness = 1 sliderStroke.Parent = sliderFrame local label = Instance.new("TextLabel") label.Text = name .. ": " .. settingTable[settingKey] label.Size = UDim2.new(1, -10, 0, 20) label.Position = UDim2.new(0, 5, 0, 5) label.BackgroundTransparency = 1 label.TextColor3 = COLORS.Primary label.Font = Enum.Font.GothamBold label.TextSize = 12 label.TextXAlignment = Enum.TextXAlignment.Left label.Parent = sliderFrame local sliderTrack = Instance.new("Frame") sliderTrack.Size = UDim2.new(1, -10, 0, 10) sliderTrack.Position = UDim2.new(0, 5, 0, 30) sliderTrack.BackgroundColor3 = COLORS.Background sliderTrack.Parent = sliderFrame local trackCorner = Instance.new("UICorner") trackCorner.CornerRadius = UDim.new(1, 0) trackCorner.Parent = sliderTrack local sliderFill = Instance.new("Frame") local fillPercentage = (settingTable[settingKey] - minValue) / (maxValue - minValue) sliderFill.Size = UDim2.new(fillPercentage, 0, 1, 0) sliderFill.BackgroundColor3 = COLORS.Primary sliderFill.Parent = sliderTrack local fillCorner = Instance.new("UICorner") fillCorner.CornerRadius = UDim.new(1, 0) fillCorner.Parent = sliderFill local sliderButton = Instance.new("TextButton") sliderButton.Text = "" sliderButton.Size = UDim2.new(1, 0, 2, 0) sliderButton.Position = UDim2.new(0, 0, -0.5, 0) sliderButton.BackgroundTransparency = 1 sliderButton.Parent = sliderTrack local function updateSliderValue(xPos) local relativeX = math.clamp(xPos, 0, sliderTrack.AbsoluteSize.X) local value = math.floor(minValue + (relativeX / sliderTrack.AbsoluteSize.X) * (maxValue - minValue)) settingTable[settingKey] = value label.Text = name .. ": " .. value local newFillPercentage = (value - minValue) / (maxValue - minValue) sliderFill.Size = UDim2.new(newFillPercentage, 0, 1, 0) if settingTable == Settings.FOV then updateFOV() end end sliderButton.MouseButton1Down:Connect(function() local connection connection = RunService.RenderStepped:Connect(function() if not UserInputService:IsMouseButtonPressed(Enum.UserInputType.MouseButton1) then connection:Disconnect() return end local mousePos = UserInputService:GetMouseLocation() local trackPos = sliderTrack.AbsolutePosition local xPos = mousePos.X - trackPos.X updateSliderValue(xPos) end) end) return sliderFrame end -- Упрощенный редактор цветов без ручного ввода RGB local function createColorEditor() local colorEditor = Instance.new("Frame") colorEditor.Size = UDim2.new(0, 320, 0, 400) colorEditor.Position = UDim2.new(0.5, -160, 0.5, -200) colorEditor.BackgroundColor3 = COLORS.Background colorEditor.BackgroundTransparency = 0.05 colorEditor.Visible = false colorEditor.ZIndex = 100 colorEditor.Parent = screenGui local editorCorner = Instance.new("UICorner") editorCorner.CornerRadius = UDim.new(0, 15) editorCorner.Parent = colorEditor local editorStroke = Instance.new("UIStroke") editorStroke.Color = COLORS.Border editorStroke.Thickness = 2 editorStroke.Parent = colorEditor local title = Instance.new("TextLabel") title.Text = "🎨 Редактор цветов" title.Size = UDim2.new(1, -20, 0, 40) title.Position = UDim2.new(0, 10, 0, 10) title.BackgroundTransparency = 1 title.TextColor3 = COLORS.Primary title.Font = Enum.Font.GothamBlack title.TextSize = 18 title.ZIndex = 101 title.Parent = colorEditor -- Превью цвета local colorPreview = Instance.new("Frame") colorPreview.Size = UDim2.new(0, 60, 0, 60) colorPreview.Position = UDim2.new(1, -70, 0, 10) colorPreview.BackgroundColor3 = Settings.Visuals.ChamsColor colorPreview.ZIndex = 101 colorPreview.Parent = colorEditor local previewCorner = Instance.new("UICorner") previewCorner.CornerRadius = UDim.new(0, 8) previewCorner.Parent = colorPreview local previewStroke = Instance.new("UIStroke") previewStroke.Color = COLORS.Primary previewStroke.Thickness = 2 previewStroke.Parent = colorPreview -- Основные цвета (черный и белый внизу) local basicColors = { {Color = Color3.new(1, 0, 0), Name = "Красный"},    -- Красный {Color = Color3.new(0, 1, 0), Name = "Зеленый"},    -- Зеленый {Color = Color3.new(0, 0, 1), Name = "Синий"},      -- Синий {Color = Color3.new(1, 1, 0), Name = "Желтый"},     -- Желтый {Color = Color3.new(1, 0, 1), Name = "Пурпурный"},  -- Пурпурный {Color = Color3.new(0, 1, 1), Name = "Голубой"},    -- Голубой {Color = Color3.new(1, 1, 1), Name = "Белый"},      -- Белый {Color = Color3.new(0, 0, 0), Name = "Черный"}      -- Черный } local basicColorsFrame = Instance.new("Frame") basicColorsFrame.Size = UDim2.new(1, -20, 0, 40) basicColorsFrame.Position = UDim2.new(0, 10, 1, -120) -- Переместил вниз basicColorsFrame.BackgroundTransparency = 1 basicColorsFrame.ZIndex = 101 basicColorsFrame.Parent = colorEditor -- Создаем кнопки основных цветов for i, colorData in ipairs(basicColors) do local colorButton = Instance.new("TextButton") colorButton.Size = UDim2.new(0, 30, 0, 30) colorButton.Position = UDim2.new(0, (i-1) * 35, 0, 0) colorButton.BackgroundColor3 = colorData.Color colorButton.BorderSizePixel = 0 colorButton.AutoButtonColor = false colorButton.Text = "" colorButton.ZIndex = 102 colorButton.Parent = basicColorsFrame local colorCorner = Instance.new("UICorner") colorCorner.CornerRadius = UDim.new(0, 6) colorCorner.Parent = colorButton local colorStroke = Instance.new("UIStroke") colorStroke.Color = COLORS.Primary colorStroke.Thickness = 1 colorStroke.Parent = colorButton -- Подсказка при наведении local tooltip = Instance.new("TextLabel") tooltip.Text = colorData.Name tooltip.Size = UDim2.new(0, 60, 0, 20) tooltip.Position = UDim2.new(0, -15, -25, 0) tooltip.BackgroundColor3 = COLORS.Surface tooltip.TextColor3 = COLORS.Primary tooltip.Font = Enum.Font.Gotham tooltip.TextSize = 10 tooltip.Visible = false tooltip.ZIndex = 103 tooltip.Parent = colorButton local tooltipCorner = Instance.new("UICorner") tooltipCorner.CornerRadius = UDim.new(0, 4) tooltipCorner.Parent = tooltip colorButton.MouseEnter:Connect(function() tooltip.Visible = true end) colorButton.MouseLeave:Connect(function() tooltip.Visible = false end) colorButton.MouseButton1Click:Connect(function() Settings.Visuals.ChamsColor = colorData.Color colorPreview.BackgroundColor3 = colorData.Color -- Обновляем все ESP элементы for target, _ in pairs(espCache) do if chamsCache[target] then chamsCache[target].FillColor = colorData.Color chamsCache[target].OutlineColor = colorData.Color end end end) end -- Палитра цветов (основная сетка) local colorGrid = Instance.new("Frame") colorGrid.Size = UDim2.new(1, -20, 0, 200) -- Увеличил высоту colorGrid.Position = UDim2.new(0, 10, 0, 60) colorGrid.BackgroundTransparency = 1 colorGrid.ZIndex = 101 colorGrid.Parent = colorEditor -- Создаем цветовую сетку 8x8 local cellSize = 25 local spacing = 3 local colorsPerRow = 8 for i = 0, 63 do local row = math.floor(i / colorsPerRow) local col = i % colorsPerRow local hue = (i % colorsPerRow) / colorsPerRow local saturation = 1.0 local lightness = 1.0 - (row / colorsPerRow) * 0.7 local color = Color3.fromHSV(hue, saturation, lightness) local colorCell = Instance.new("TextButton") colorCell.Size = UDim2.new(0, cellSize, 0, cellSize) colorCell.Position = UDim2.new(0, col * (cellSize + spacing), 0, row * (cellSize + spacing)) colorCell.BackgroundColor3 = color colorCell.BorderSizePixel = 0 colorCell.AutoButtonColor = false colorCell.Text = "" colorCell.ZIndex = 102 colorCell.Parent = colorGrid local cellCorner = Instance.new("UICorner") cellCorner.CornerRadius = UDim.new(0, 4) cellCorner.Parent = colorCell local cellStroke = Instance.new("UIStroke") cellStroke.Color = COLORS.Primary cellStroke.Thickness = 1 cellStroke.Parent = colorCell colorCell.MouseButton1Click:Connect(function() Settings.Visuals.ChamsColor = color colorPreview.BackgroundColor3 = color -- Обновляем все ESP элементы for target, _ in pairs(espCache) do if chamsCache[target] then chamsCache[target].FillColor = color chamsCache[target].OutlineColor = color end end end) end -- Кнопка закрытия local closeButton = Instance.new("TextButton") closeButton.Text = "Закрыть" closeButton.Size = UDim2.new(1, -20, 0, 35) closeButton.Position = UDim2.new(0, 10, 1, -70) closeButton.BackgroundColor3 = COLORS.Surface closeButton.TextColor3 = COLORS.Primary closeButton.Font = Enum.Font.GothamBold closeButton.TextSize = 14 closeButton.ZIndex = 101 closeButton.Parent = colorEditor local closeCorner = Instance.new("UICorner") closeCorner.CornerRadius = UDim.new(0, 8) closeCorner.Parent = closeButton local closeStroke = Instance.new("UIStroke") closeStroke.Color = COLORS.Border closeStroke.Thickness = 1 closeStroke.Parent = closeButton closeButton.MouseButton1Click:Connect(function() colorEditor.Visible = false end) return colorEditor end -- Функция для создания кнопки с шестерёнкой local function createSettingsButton(yPos, parentFrame, onClick) local button = Instance.new("TextButton") button.Text = "⚙️ Настройки цветов" button.Size = UDim2.new(1, -20, 0, 35) button.Position = UDim2.new(0, 10, 0, yPos) button.BackgroundColor3 = COLORS.Surface button.TextColor3 = COLORS.Primary button.Font = Enum.Font.GothamBold button.TextSize = 14 button.Parent = parentFrame local buttonCorner = Instance.new("UICorner") buttonCorner.CornerRadius = UDim.new(0, 8) buttonCorner.Parent = button local buttonStroke = Instance.new("UIStroke") buttonStroke.Color = COLORS.Border buttonStroke.Thickness = 1 buttonStroke.Parent = button button.MouseButton1Click:Connect(onClick) return button end -- Функция для создания интерфейса меню local function createCheatMenu() if mainFrame then mainFrame:Destroy() end local mainFrameInstance = Instance.new("Frame") mainFrameInstance.Name = "WinterESP" mainFrameInstance.Size = UDim2.new(0, 350, 0, 500) mainFrameInstance.Position = UDim2.new(0.02, 0, 0.02, 0) mainFrameInstance.BackgroundColor3 = COLORS.Background mainFrameInstance.BackgroundTransparency = 0.05 mainFrameInstance.BorderSizePixel = 0 mainFrameInstance.ClipsDescendants = true mainFrameInstance.Visible = isMenuVisible mainFrameInstance.Parent = screenGui mainFrame = mainFrameInstance local UICorner = Instance.new("UICorner") UICorner.CornerRadius = UDim.new(0, 15) UICorner.Parent = mainFrameInstance local stroke = Instance.new("UIStroke") stroke.Color = COLORS.Border stroke.Thickness = 2 stroke.Parent = mainFrameInstance local titleBar = Instance.new("Frame") titleBar.Name = "TitleBar" titleBar.Size = UDim2.new(1, -20, 0, 50) titleBar.Position = UDim2.new(0, 10, 0, 10) titleBar.BackgroundColor3 = COLORS.Surface titleBar.BackgroundTransparency = 0.1 titleBar.Parent = mainFrameInstance local titleCorner = Instance.new("UICorner") titleCorner.CornerRadius = UDim.new(0, 10) titleCorner.Parent = titleBar local titleStroke = Instance.new("UIStroke") titleStroke.Color = COLORS.Border titleStroke.Thickness = 2 titleStroke.Parent = titleBar local minimizeButton = Instance.new("TextButton") minimizeButton.Name = "MinimizeButton" minimizeButton.Text = "▲" minimizeButton.Size = UDim2.new(0, 25, 0, 25) minimizeButton.Position = UDim2.new(1, -30, 0.5, -12) minimizeButton.AnchorPoint = Vector2.new(1, 0.5) minimizeButton.BackgroundColor3 = COLORS.Surface minimizeButton.TextColor3 = COLORS.Primary minimizeButton.Font = Enum.Font.GothamBold minimizeButton.TextSize = 14 minimizeButton.Parent = titleBar local minimizeCorner = Instance.new("UICorner") minimizeCorner.CornerRadius = UDim.new(0, 6) minimizeCorner.Parent = minimizeButton local minimizeStroke = Instance.new("UIStroke") minimizeStroke.Color = COLORS.Border minimizeStroke.Thickness = 1 minimizeStroke.Parent = minimizeButton local title = Instance.new("TextLabel") title.Text = "vrv1k & lfr script." title.Size = UDim2.new(1, -40, 0.6, 0) title.Position = UDim2.new(0, 10, 0, 0) title.BackgroundTransparency = 1 title.TextColor3 = COLORS.Primary title.Font = Enum.Font.GothamBlack title.TextSize = 16 title.TextXAlignment = Enum.TextXAlignment.Left title.Parent = titleBar local subtitle = Instance.new("TextLabel") subtitle.Text = "by Vorv1k & lfuer" subtitle.Size = UDim2.new(1, -40, 0.4, 0) subtitle.Position = UDim2.new(0, 10, 0.6, 0) subtitle.BackgroundTransparency = 1 subtitle.TextColor3 = COLORS.Secondary subtitle.Font = Enum.Font.GothamBold subtitle.TextSize = 10 subtitle.TextXAlignment = Enum.TextXAlignment.Left subtitle.Parent = titleBar local contentContainer = Instance.new("Frame") contentContainer.Name = "ContentContainer" contentContainer.Size = UDim2.new(1, 0, 1, -70) contentContainer.Position = UDim2.new(0, 0, 0, 70) contentContainer.BackgroundTransparency = 1 contentContainer.Parent = mainFrameInstance local tabContainer = Instance.new("Frame") tabContainer.Size = UDim2.new(1, -20, 0, 40) tabContainer.Position = UDim2.new(0, 10, 0, 0) tabContainer.BackgroundTransparency = 1 tabContainer.Parent = contentContainer local tabs = {"ESP", "Visuals", "Misc"} local currentTab = "ESP" local contentFrames = {} for i, tabName in pairs(tabs) do local tabButton = Instance.new("TextButton") tabButton.Text = tabName tabButton.Size = UDim2.new(0.3, 0, 1, 0) tabButton.Position = UDim2.new(0.3 * (i-1), 0, 0, 0) tabButton.BackgroundColor3 = i == 1 and COLORS.Surface or COLORS.Background tabButton.TextColor3 = COLORS.Primary tabButton.Font = Enum.Font.GothamBold tabButton.TextSize = 14 tabButton.Parent = tabContainer local tabCorner = Instance.new("UICorner") tabCorner.CornerRadius = UDim.new(0, 8) tabCorner.Parent = tabButton local tabStroke = Instance.new("UIStroke") tabStroke.Color = COLORS.Border tabStroke.Thickness = 1 tabStroke.Parent = tabButton local contentFrame = Instance.new("ScrollingFrame") contentFrame.Size = UDim2.new(1, -20, 1, -55) contentFrame.Position = UDim2.new(0, 10, 0, 50) contentFrame.BackgroundTransparency = 1 contentFrame.ScrollBarThickness = 4 contentFrame.ScrollBarImageColor3 = COLORS.Primary contentFrame.CanvasSize = UDim2.new(0, 0, 0, 0) contentFrame.Visible = i == 1 contentFrame.Parent = contentContainer contentFrames[tabName] = contentFrame tabButton.MouseButton1Click:Connect(function() currentTab = tabName for name, frame in pairs(contentFrames) do frame.Visible = (name == tabName) -- Обновляем цвет активной вкладки for _, btn in pairs(tabContainer:GetChildren()) do if btn:IsA("TextButton") then btn.BackgroundColor3 = btn.Text == tabName and COLORS.Surface or COLORS.Background end end end end) end -- Создаем редактор цветов local colorEditor = createColorEditor() -- Вкладка ESP local espContent = contentFrames["ESP"] local yPos = 0 createToggle("ESP Включено", Settings.ESP, "Enabled", yPos, espContent) yPos += 40 createToggle("Показывать врагов", Settings.ESP, "ShowEnemies", yPos, espContent) yPos += 40 createToggle("Показывать дронов", Settings.ESP, "ShowDrones", yPos, espContent) yPos += 40 createToggle("Показывать имена", Settings.ESP, "ShowNames", yPos, espContent) yPos += 40 createSlider("Дальность ESP", Settings.ESP, "MaxDistance", 100, 2000, yPos, espContent) yPos += 55 createSlider("Дальность дронов", Settings.ESP, "DroneDistance", 100, 1000, yPos, espContent) yPos += 55 espContent.CanvasSize = UDim2.new(0, 0, 0, yPos) -- Вкладка Visuals local visualsContent = contentFrames["Visuals"] yPos = 0 createToggle("Обводка моделей", Settings.Visuals, "OutlineModels", yPos, visualsContent) yPos += 40 createToggle("Показывать здоровье", Settings.Visuals, "ShowHealth", yPos, visualsContent) yPos += 40 createToggle("Показывать дистанцию", Settings.Visuals, "ShowDistance", yPos, visualsContent) yPos += 40 createToggle("Chams", Settings.Visuals, "Chams", yPos, visualsContent) yPos += 40 createToggle("X-Ray", Settings.Visuals, "XRay", yPos, visualsContent) yPos += 40 -- Кнопка с шестерёнкой для открытия редактора цветов createSettingsButton(yPos, visualsContent, function() colorEditor.Visible = true end) yPos += 45 visualsContent.CanvasSize = UDim2.new(0, 0, 0, yPos) -- Вкладка Misc local miscContent = contentFrames["Misc"] yPos = 0 createToggle("Изменение FOV", Settings.FOV, "Enabled", yPos, miscContent) yPos += 40 createSlider("Значение FOV", Settings.FOV, "Value", 30, 120, yPos, miscContent) yPos += 55 miscContent.CanvasSize = UDim2.new(0, 0, 0, yPos) local bottomPanel = Instance.new("Frame") bottomPanel.Size = UDim2.new(1, -20, 0, 30) bottomPanel.Position = UDim2.new(0, 10, 1, -35) bottomPanel.BackgroundColor3 = COLORS.Surface bottomPanel.BackgroundTransparency = 0.1 bottomPanel.Parent = contentContainer local bottomCorner = Instance.new("UICorner") bottomCorner.CornerRadius = UDim.new(0, 8) bottomCorner.Parent = bottomPanel local bottomStroke = Instance.new("UIStroke") bottomStroke.Color = COLORS.Border bottomStroke.Thickness = 1 bottomStroke.Parent = bottomPanel local statusLabel = Instance.new("TextLabel") statusLabel.Text = "🟢 СИСТЕМА АКТИВНА | FPS: 60" statusLabel.Size = UDim2.new(1, 0, 1, 0) statusLabel.BackgroundTransparency = 1 statusLabel.TextColor3 = COLORS.Success statusLabel.Font = Enum.Font.GothamBold statusLabel.TextSize = 11 statusLabel.Parent = bottomPanel -- Оптимизированное обновление FPS local fpsUpdateTime = 0 spawn(function() while true do wait(1) if statusLabel and statusLabel.Parent then local fps = math.floor(1/RunService.RenderStepped:Wait()) statusLabel.Text = "🟢 СИСТЕМА АКТИВНА | FPS: " .. fps end end end) -- Обработчик сворачивания minimizeButton.MouseButton1Click:Connect(function() isMinimized = not isMinimized local tweenInfo = TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.Out) if isMinimized then local sizeTween = TweenService:Create(mainFrameInstance, tweenInfo, {Size = UDim2.new(0, 350, 0, 70)}) sizeTween:Play() contentContainer.Visible = false minimizeButton.Text = "▼" else local sizeTween = TweenService:Create(mainFrameInstance, tweenInfo, {Size = UDim2.new(0, 350, 0, 500)}) sizeTween:Play() contentContainer.Visible = true minimizeButton.Text = "▲" end end) -- Перетаскивание окна local dragging = false local dragStart, startPos titleBar.InputBegan:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseButton1 then dragging = true dragStart = input.Position startPos = mainFrameInstance.Position end end) titleBar.InputEnded:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseButton1 then dragging = false end end) UserInputService.InputChanged:Connect(function(input) if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then local delta = input.Position - dragStart mainFrameInstance.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y) end end) return mainFrameInstance end -- Оптимизированная функция для перезапуска скрипта при смерти local function setupRespawn() local function onCharacterAdded(character) if character:WaitForChild("Humanoid") then wait(1) -- Очищаем кэши for target, data in pairs(espCache) do if data.Outline then data.Outline:Destroy() end data.Frame:Destroy() end espCache = {} for target, highlight in pairs(chamsCache) do highlight:Destroy() end chamsCache = {} -- Обновляем кэш камеры camera = workspace.CurrentCamera createGUI() createCheatMenu() end end localPlayer.CharacterAdded:Connect(onCharacterAdded) if localPlayer.Character then onCharacterAdded(localPlayer.Character) end end -- Обработчик горячих клавиш local function setupHotkeys() UserInputService.InputBegan:Connect(function(input, gameProcessed) if gameProcessed then return end if input.KeyCode == Enum.KeyCode.Escape then isMenuVisible = not isMenuVisible if mainFrame then mainFrame.Visible = isMenuVisible end elseif input.KeyCode == Enum.KeyCode.Insert then Settings.ESP.Enabled = not Settings.ESP.Enabled elseif input.KeyCode == Enum.KeyCode.Home then Settings.Visuals.Chams = not Settings.Visuals.Chams elseif input.KeyCode == Enum.KeyCode.End then Settings.Visuals.XRay = not Settings.Visuals.XRay applyXRay() elseif input.KeyCode == Enum.KeyCode.PageUp then Settings.ESP.ShowDrones = not Settings.ESP.ShowDrones end end) end -- Главный цикл local function main() -- Инициализируем кэш камеры camera = workspace.CurrentCamera if camera then originalFOV = camera.FieldOfView end createGUI() createCheatMenu() setupRespawn() setupHotkeys() -- Оптимизированный основной цикл connections.render = RunService.RenderStepped:Connect(function() updateESPSystem() updateFOV() end) print("🟢 vrv1k & lfr script. загружена!") print("🎮 Горячие клавиши:") print("   ESC - Показать/скрыть меню") print("   INSERT - Включить/выключить ESP") print("   HOME - Включить/выключить Chams") print("   END - Включить/выключить X-Ray") print("   PAGE UP - Включить/выключить ESP дронов") print("🎨 Упрощенный редактор цветов доступен в разделе Visuals") print("С уважением Lfuer&Vorv1k") end -- Запуск main()
